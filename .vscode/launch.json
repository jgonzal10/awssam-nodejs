{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach",
            "address": "localhost",
            "port": 5858,
            "localRoot": "${workspaceRoot}/create-developers",
            "protocol": "inspector",
            "remoteRoot": "/var/task",
        }
    ]
}